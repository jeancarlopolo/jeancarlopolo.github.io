import 'package:flutter/material.dart';

class Constantes {
  static const claroBackground = Color(0xFFF7F0DF);
  static const claroHighlight = Color(0xFF3d382b);

  static const escuroBackground = Color(0xFF21023A);
  static const escuroHighlight = Color(0xFFd7adf2);

  static const inicioAnimacaoBotaoTema = 40 / 134;
  static const fimAnimacaoBotaoTema = 80 / 134;
  static const coeficienteBotao = 2.3;
}
