import 'package:flutter/material.dart';

class FormacaoPage extends StatelessWidget {
  const FormacaoPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Text('formação');
  }
}